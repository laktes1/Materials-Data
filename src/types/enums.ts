export enum fileTypes {
    CIF = "CIF",
    OUT = "OUT"
}

export enum dataViewFormats {
    PARSED = "parsed",
    DIVIDED = "divided",
}

export enum type_out {
    OPT = "optimization",
    HESS = "hessian",
    RAM = "raman",
    SP = "single_point"
}
